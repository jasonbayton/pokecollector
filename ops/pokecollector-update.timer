[Unit]
Description=Check for a new PokeCollector fork tag

[Timer]
# Signed GitHub push webhooks are the normal release trigger.  This remains a
# low-frequency recovery path in case GitHub cannot deliver a notification.
OnBootSec=3min
OnUnitActiveSec=1h
AccuracySec=5min

[Install]
WantedBy=timers.target
