[Service]
# Retained scan uploads must outlive a release checkout and remain available
# to both the candidate and a rolled-back revision.
Environment=SCAN_UPLOAD_DIR=/var/lib/pokecollector/scan-uploads
